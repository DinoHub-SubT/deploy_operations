# ////////////////////////////////////////////////////////////////////////////
- basestation:
# ////////////////////////////////////////////////////////////////////////////
  # general platform type
  - export PLATFORM=basestation

  # exact name of remote/local system
  - +env:ROBOT=basestation

  # setup docker context
  - +import:env/platform.env
  - set_docker_dctx

  # extend the basestation workspace
  - +extend: workspaces/basestation
