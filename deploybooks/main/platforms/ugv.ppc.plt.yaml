- ppc:
  - +ssh:{ $ROBOT.ppc }
  - export PLATFORM_COMPUTER_NAME=ppc
  
  # ugv computer split
  - +import:env/split.env

  # setup docker context
  - export DCTX=robot-$ROBOT-ppc

  # extend remote rsync actions
  - +extend: common/sync

  # extend ugv ppc workspace
  - +extend: workspaces/ugv.ppc
