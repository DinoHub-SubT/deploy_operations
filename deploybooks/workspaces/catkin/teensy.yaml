- build:
  - cd $SUBT_PATH/uav/hardware/ds_teensy_server
  - rm -rf build || true
  - mkdir build
  - cd build
  - cmake ..
  - make -j4

- clean:
  - rm -rf $SUBT_PATH/uav/hardware/ds_teensy_server/build
